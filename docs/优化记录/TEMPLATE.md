# <标题>

**日期**：YYYY-MM-DD  
**Workstream**：WS?（例如 WS0 Guardrails）  
**范围**：backend / frontend / desktop / repo / docs  
**风险等级**：A / B / C / D  
**目标**：稳定性 / 可维护性 / 体量控制 / 质量提升  
**关联计划**：`docs/plans/...`（如有）  
**关联提交**：起止 SHA（如 `BASE..HEAD`）  

## 背景

写清楚为什么要做这件事，当前痛点是什么，约束是什么（例如必须可回滚、必须保持契约不变）。

## 本阶段策略与约束

写清楚本阶段的策略（例如先建门禁再重构、先冻结后删除）与明确不做的事（避免范围漂移）。

## 变更清单（按类别）

每条都写清楚“改了什么”和“为什么”，并点名文件/模块。

## 删除/迁移/冻结证据链（若适用）

删除类变更必须记录证据链，至少包含：

- 静态引用扫描（rg/导入/路由/配置 key）
- 动态引用排查（注册表/反射/字符串加载）
- 风险与替代路径（如先冻结再删）

## 验证证据（必须）

写明“跑了什么命令”和“结果摘要”。推荐统一使用 `make verify`，也可以按模块拆分。

## 产出与指标

建议至少记录：

- 代码体量变化（文件数/LOC/重复逻辑减少情况）
- 覆盖率或关键回归测试补齐情况（如有）
- 稳定性指标（例如修复了哪些 flake/边界问题，或减少了哪些回归点）
- 风险点与回滚点（`git revert <sha>` 或开关回退）

## 遗留问题与下一步

写清楚还剩什么没做、为什么、下一步打算怎么推进。

